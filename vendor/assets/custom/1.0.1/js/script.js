function relocation(direction){ if(direction == 'index'){ window.location = conf.domain }}

var fnc = {
  snap(snap){
    if((snap != '') && (snap.length > 0)){
      return true;
    }else{
      return false;
    }
  }
}
